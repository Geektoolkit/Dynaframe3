@inject HttpClient Client 

<button class="btn btn-warning btn-lg" @onclick="OnClick">Exit Dynaframe</button>

@code {
    private async Task OnClick()
    {
        var resp = await Client.SendAsync(new HttpRequestMessage(HttpMethod.Post, "commands/EXITAPP"));

        resp.EnsureSuccessStatusCode();
    }
}
